# 003：價目 UI 位置從「觀測」hub 到 Model 區
> 日期：2026-05-27

## 轉移
- 舊（superseded）：spec 016 的 plan 決定把價目 admin UI 放「觀測」hub 加一個「價目」分頁（理由：價目屬
  計費/觀測領域、且守階段 5.1 不增頂層 nav）。
- 新：落地當天即被 #17 搬到 **Model 區**（`/admin/model/prices`），並讓會員/管理員的模型目錄與分配詳情
  直接顯示現價。

## 為什麼變
同階段內對 plan 位置決策的即時反轉。**(推測)** 因價目與「看 model」的心智更近，放 Model 區比觀測更順手
（同一件事收斂到使用者最會去找它的地方）。

## 狀態
✅ 已採用。屬 projection-級的 UI 位置調整，但因它推翻了 plan 明載的決策、故記為轉移。
