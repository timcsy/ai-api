# Arc 8（帳號登入/逐筆記錄/OAuth/批次/多端點維護/部署硬化/叢集遷移）未決項快照

> 怎麼冒出來的：migrate 從 arc-8〈開放〉搬下（截至 `9ec5eea`，2026-08-26，最後一弧）。這些多是**現行仍活的**未決項。

- 〔仍開放/明確 descope〕**whisper 無 usage → 逐筆 token 為 null（按時長計費未做）**：無音訊庫、不新增套件；
  diarize/transcribe 有 token 已足夠。未來若要精確需音訊時長來源。
- 〔部分閉合〕**STT/responses 的 usage 映射精度**：TokenMeter 已補 `input→prompt`/`output→completion` 修好
  diarize/transcribe（本為 0）；更廣的映射精度為既有計費議題、非 502。
- 〔仍開放/(推測未動工)〕**多 provider 憑證／負載分散**：round-robin 已具，但單把 Azure key 時無效（2026-06-29
  事故即此）；未來量再大需多把 key 或連線池中介（PgBouncer）。
- 〔仍開放〕**OAuth 未來可加金鑰到期日/輪替**：spec 057 明列「長期金鑰、不做 refresh」為凍結範圍；到期日/輪替
  列「未來可加、非本功能」。
- 〔仍開放〕**image_edit / search 真分支**待非 Azure provider（FLUX/Stability、Perplexity/Tavily）；
  **video / vector_store** 按需評估、多半 descope。
- 〔進行中規劃〕**階段 35 供應鏈 / starlette+FastAPI major bump**：`.trivyignore` 暫掛兩個 starlette CVE，
  待 FastAPI 1.x 解鎖。
- 〔仍開放/優化〕**推理模型可預先不送 temperature**：目錄已有 `reasoning` 標記，未來可據此預先不送 temperature
  省一次 400→retry 往返——但反應式重試是必留的保險網。
