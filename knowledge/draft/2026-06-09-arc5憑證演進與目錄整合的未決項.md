# Arc 5（憑證 M:N/Codex 安裝/目錄↔LiteLLM/responses 偵測/測模型/應用商店）未決項快照

> 怎麼冒出來的：migrate 從 arc-5〈開放〉搬下（截至 `f9a7254`，2026-06-09）。

- 〔仍開放〕**同 model 兩分配的歸戶歧義**：靠 `UNIQUE(credential_id, resource_model)` 在 key 層禁止；但「同
  model 多分配（不同用途）」仍成立於分配層。(推測) 未來若一把 key 想同時涵蓋同 model 的兩筆不同用途分配，
  需要新的區分維度（現以「一把 key 內 model 不重複」迴避）。
- 〔仍開放/已知限制〕**手動 embedding 種類判定**：litellm 把 embedding 映成 output `["text"]` 與 chat 撞型；
  純手動建立（無 litellm mode）的 embedding 會退為 chat、測試時明確失敗。作者標「已知限制」未解。
- 〔仍開放〕**STT 自動測試**：v1 不做（顯示「尚不支援自動測試」）。
- 〔仍開放〕**一般 OpenAI 應用進應用目錄**：v1 只 Codex；Continue/OpenWebUI/LibreChat + 「手動建金鑰貼上」
  路徑留待未來（屆時「應用目錄」名稱才名副其實）。
- 〔仍開放〕**VS Code 以外 IDE 擴充自動安裝**：無法可靠確認 extension id / 跨平台 → v1 link-only、未解。
